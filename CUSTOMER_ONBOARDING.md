# Astro Destiny Analyzer — 客戶首次使用指南

> **版本**：v2.0.4  
> **適用對象**：首次收到 ZIP 壓縮檔的使用者

---

## 〇、語言選擇（V2.0.4 新功能）

應用程式啟動後，左側側欄頂端有語言選單，支援：
- **繁體中文**（預設）
- **English**
- **ไทย**（泰文）
- **日本語**

選擇語言後頁面會自動切換，不影響已輸入的出生資料。

---

## 一、ZIP 解壓縮

1. 找到您收到的 ZIP 壓縮檔（例如：`astro_destiny_analyzer_v2.0.3_customer.zip`）
2. 在檔案上按右鍵 → 「解壓縮至目前位置」或「全部解壓縮」
3. 進入解壓縮後產生的資料夾
4. 確認資料夾內有 `setup.bat`、`run.bat`、`CUSTOMER_README.md` 等檔案

---

## 二、第一次安裝

1. **雙擊 `setup.bat`**
2. 等待安裝完成（畫面會顯示套件安裝進度）
3. 安裝完成後視窗會自動關閉或顯示完成訊息

> **注意**：安裝過程需要網路連線以下載 Python 套件。若安裝中斷，請重新雙擊 `setup.bat`。

---

## 三、啟動應用程式

1. **雙擊 `run.bat`**
2. 瀏覽器會自動開啟 `http://localhost:8501`
3. 若未自動開啟，請手動在瀏覽器輸入 `http://localhost:8501`
4. 保持命令視窗（黑色視窗）開啟，關閉它會停止程式

---

## 四、建立第一份命盤報告

### 步驟 1 — 輸入資料

1. 點選左側選單「**📝 輸入資料**」
2. 填入以下資料：
   - **姓名**：任意名稱（可匿名）
   - **出生年月日**：精確日期
   - **出生時間**：若知道精確時間請勾選並填入；若不確定可略過
   - **出生地**：選擇台灣城市，或手動輸入其他地點
   - **出生國家**：預設台灣，可自由修改
3. 確認資料無誤後，點擊「**儲存並繼續**」

### 步驟 2 — 計算命盤

1. 前往「**🔮 計算命盤**」
2. 點擊「**開始計算**」按鈕
3. 系統會自動產生以下分析：
   - 西洋占星（行星、宮位、相位）
   - 八字命理（四柱、五行、大運）
   - 紫微斗數（宮位、主星）
   - 人類圖（Type、Profile、Authority）
   - 整合摘要

### 步驟 3 — 報告預覽

1. 前往「**📄 報告預覽**」
2. 切換「整合分析（互動式）」或「Markdown 原文」查看報告
3. 確認報告內容符合需求

### 步驟 4 — 匯出報告

1. 前往「**📤 匯出**」
2. 選擇匯出格式（建議優先使用 HTML 或 Word）
3. 點擊下載按鈕儲存檔案

---

## 五、建立第一份合盤報告

1. 點選左側選單「**💕 合盤分析**」
2. 選擇關係類型（情侶 / 婚姻 / 合作夥伴 / 親子 / 朋友 / 同事）
3. 輸入 A 方資料（或使用目前命盤）
4. 輸入 B 方資料
5. 點擊「**💕 產生合盤分析**」
6. 查看合盤結果，並從合盤頁匯出報告

---

## 六、取得免費摘要

1. 點選左側選單「**🎁 免費報告**」
2. 選擇報告類型
3. 填入 Email（僅儲存於本機，不會外送）與基本資料
4. 點擊送出，取得免費命盤摘要

---

## 七、匯出格式建議

| 格式 | 說明 | 適合用途 |
|------|------|---------|
| 🌐 **HTML** | 最穩定，單一自含檔案 | 交付客戶、列印、長期保存 |
| 📘 **Word** | 可編輯 .docx 格式 | 顧問人工修稿、客戶交付 |
| 📝 **Markdown** | 純文字格式 | 保存原文、交給 AI 二次整理 |
| 📕 **PDF**（選用） | 需要安裝 WeasyPrint | 固定格式交付 |

> **建議**：若 PDF 功能未啟用，優先使用 **HTML** 或 **Word**。功能完整，不需額外安裝。
>
> 若要啟用 PDF：**雙擊 `install_pdf_support.bat`**，安裝完成後重新啟動 `run.bat`。

---

## 八、常見問題

| 問題 | 解決方式 |
|------|---------|
| setup.bat 顯示錯誤 | 確認已安裝 Python（建議 3.10 以上）；重新雙擊 setup.bat |
| run.bat 無法啟動 | 先執行 setup.bat，確認安裝完成 |
| 瀏覽器沒有自動開啟 | 手動在瀏覽器輸入 `http://localhost:8501` |
| Word 匯出失敗 | 重新執行 setup.bat，確認 python-docx 已安裝 |
| 頁面無法載入 | 確認 run.bat 命令視窗仍在執行中，不要關閉它 |
| 中文顯示亂碼 | 使用 run.bat 啟動，確保終端機使用 UTF-8 編碼 |

---

## 九、本機資料與隱私

- 所有資料存儲在本機 `data/` 資料夾
- **不會自動上傳至任何伺服器**
- 不會自動寄送 Email
- 不會串接付款系統或 CRM
- 請自行備份 `data/` 資料夾以防資料遺失
- 解除安裝：直接刪除整個資料夾即可

---

## 十、免責聲明

本工具定位為「自我探索與娛樂型命盤分析工具」。

- 不構成醫療診斷或治療建議
- 不構成法律意見
- 不構成投資或財務建議
- 不代表絕對命運預測

請以開放、探索的心態閱讀報告，最終決策仍以您自身判斷為準。

---

*Astro Destiny Analyzer v2.0.3 — 客戶首次使用指南*
